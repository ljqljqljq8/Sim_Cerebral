# Operation Log

This file records the current automation build status.

- Created project structure for Luisi 2024 CoW automation.
- Downloaded `41598_2024_58925_MOESM2_ESM.csv`.
- Downloaded and unpacked `41598_2024_58925_MOESM3_ESM.zip`.
- Confirmed extracted STL path: `data_raw/luisi2024/extracted/CerebrovascularModel.stl`.
- Confirmed the STL is already closed; preprocessing extracts existing planar caps.
- Generated 10 named pre-mesh faces and `face_map.json`.
- Generated synthetic ultrasound inlet `.flow` files.
- Generated Luisi-derived outlet RCR table.
- Generated a Python TetGen fallback mesh with about 883k tetrahedra.
- Generated `cases/cow_luisi_mvp/solver/solver.xml` with strict mesh input checks passing.
- Added `scripts/check_runtime.py` so runtime availability and the SimVascular.app Python import failure are recorded under `docs/runtime_status.json`.
- Created and validated `.venv-sv` using Python 3.9.13 with VTK 9.3.1, matching the reference SimVascular-oriented environment more closely than `.venv-cow`.
- Regenerated the fallback mesh using `.venv-sv`; current validation reports 883,200 tetrahedra and zero boundary edges.
- Found the correct SimVascular batch entrypoint from the reference project: `/Applications/SimVascular.app/Contents/Resources/simvascular --python -- <script.py>`.
- Confirmed the SimVascular wrapper can import `sv.meshing.TetGen`; direct import from the bundled Python still fails and should not be used.
- Generated an official SimVascular TetGen production mesh in `cases/cow_luisi_mvp/mesh/` with 1,450,418 tetrahedra and 271,624 points.
- Updated the official meshing script to create a tagged `surface_capped_model_face_id.vtp` using `ModelFaceID`; wall is face ID 1 and caps are face IDs 2-11.
- Disabled MMG and boundary-layer meshing by default for this SimVascular 2026 installation because MMG resets/mishandles `ModelFaceID` and boundary-layer generation fails, then can crash if retried in-process.
- Found the local svMultiPhysics executable at `/usr/local/sv/svMultiPhysics/2025-06-06/bin/svmultiphysics` and wired it into `config/case_luisi.yaml`, `scripts/check_runtime.py`, and `scripts/08_run_svmultiphysics.sh`.
- Added smoke controls: `04_generate_tetgen_mesh.py --mesh-subdir/--global-edge-size-cm/--cap-edge-size-cm`, `utils_merge_wall.py --mesh-subdir`, and `07_write_svmultiphysics_xml.py --output/--mesh-subdir/--n-steps`.
- Generated an official SimVascular smoke mesh in `cases/cow_luisi_mvp/mesh_smoke/` with 308,147 tetrahedra and 65,471 points.
- Ran `MPI_NP=4 SOLVER_XML=solver_smoke.xml bash scripts/08_run_svmultiphysics.sh`; the two-step svMultiPhysics smoke simulation completed with exit code 0.
- Ran `scripts/09_postprocess_results.py`; postprocess outputs are under `cases/cow_luisi_mvp/results/post/`.
- Final validation of the production case passes with 10 caps, 1,450,418 tetrahedra, and zero boundary edges.
- Verified the unified automation driver with:
  `MPI_NP=4 .venv-sv/bin/python scripts/run_pipeline.py --skip-download --check-runtime --with-mesh --mesh-subdir mesh_smoke --global-edge-size-cm 0.10 --cap-edge-size-cm 0.06 --solver-xml solver_smoke.xml --solver-steps 2 --save-every 1 --with-solver --validate`.
- The unified smoke run regenerated the official smoke mesh, completed two svMultiPhysics steps, wrote `result_001.vtu` and `result_002.vtu`, postprocessed flux/pressure, and passed validation.
- Started a formal production solve on the 1,450,418-tetrahedra mesh with `solver_production_save20.xml`: 3000 steps, `dt=0.001`, VTK output every 20 steps, and 7 MPI ranks. Status file: `cases/cow_luisi_mvp/solver/solver_production_save20_np7.status.json`; log: `cases/cow_luisi_mvp/solver/solver_production_save20_np7.run.log`; output directory: `cases/cow_luisi_mvp/solver/7-procs/`.
- Added a disk monitor for the production solve. It records free space in `cases/cow_luisi_mvp/solver/monitor_disk_production_save20.log` and writes `STOP_SIM` if free space drops below 5 GiB.
- Production solve status check: the run completed time step 1 and entered time step 2 (`NS 2-1`). No VTU has been written yet because this production XML saves every 20 steps. Disk space is stable around 18.6 GiB free and `STOP_SIM` has not been triggered.
- Added `scripts/check_production_solve_status.py` to parse the production solve log, count VTU outputs, report free disk space, and write `cases/cow_luisi_mvp/solver/solver_production_save20_np7.progress.json`.
- Production solve status check: the run has entered time step 3 (`NS 3-2`). No VTU has been written yet because the first saved VTU is expected at step 20. Disk space remains stable around 17.6 GiB free and `STOP_SIM` has not been triggered.
- Added `scripts/analyze_solver_log.py` to summarize nonlinear iteration progress and linear solver warnings.
- Stopped the initial `solver_production_save20.xml` run because every observed linear solve warned and no time step showed a convergence marker, which was not acceptable for the requested high-fidelity solve.
- Generated and started `solver_production_hifi_save20.xml` on the same 1,450,418-tetrahedra production mesh. It uses 3000 steps, `dt=0.001`, VTK output every 20 steps, fluid `Max_iterations=12`, linear solver `Max_iterations=30`, `NS_GM_max_iterations=25`, `NS_CG_max_iterations=800`, linear tolerance `1e-4`, and 6 MPI ranks. Status file: `cases/cow_luisi_mvp/solver/solver_production_hifi_save20_np6.status.json`; log: `cases/cow_luisi_mvp/solver/solver_production_hifi_save20_np6.run.log`; output directory: `cases/cow_luisi_mvp/solver/6-procs/`.
- Hifi production status check: step 1 reached nonlinear iteration 7 with zero linear solver warnings and residual `ri_r1=1.866e-07`; no VTU has been written yet because the first saved VTU is expected at step 20.
- Added direct ultrasound velocity support to `scripts/05_generate_inlet_waveforms.py`: inlet inputs may now use measured `mean_velocity_cm_s` or `mean_velocity_m_s` plus `diameter_mm`/`area_cm2`, which are converted to the `.flow` files required by svMultiPhysics.
- Added `config/ultrasound_velocity_example.yaml` and `docs/simulating_from_ultrasound_velocity.md` documenting the patient-velocity workflow, current inlet/outlet boundary conditions, and the limitation that the current geometry does not include ECA/STA/preauricular/CCA branches.
- Re-evaluated the requested two-step VTU output cadence using the observed smoke VTU size. Extrapolated production VTU size is about 58 MiB per file; a 3000-step run with `save_every=2` would require roughly 85 GiB, which exceeds the current available disk space. The active formal production run therefore keeps `save_every=20`, estimated at roughly 8.5 GiB for 3000 steps.
- Hifi production status check: the solve entered time step 2. `NS 2-1` produced one linear solver warning, then `NS 2-2` reduced `ri_r1` from `1.0` to `3.403e-02` without a new warning. The 6 svMultiPhysics worker processes remain CPU-active, so the current high-fidelity production solve is continuing.
- Updated `scripts/09_postprocess_results.py` so postprocessing can explicitly target a solver output directory such as `cases/cow_luisi_mvp/solver/6-procs`, and fixed boundary integral column labeling for files with `step time <faces...>` columns.
- Verified live postprocessing against the active production output using `--solver-output-dir cases/cow_luisi_mvp/solver/6-procs --post-dir cases/cow_luisi_mvp/results/post_hifi_np6_live`. Boundary flow and pressure files were processed; WSS and VTU files are still absent until later solver output.
- Hifi production status check: the run remains active on 6 MPI workers. Time step 2 has reached `NS 2-4`, with `ri_r1` reduced to `3.870e-03`; the only linear solver warning so far is still the initial `NS 2-1` warning. No production VTU has been written yet because the first VTK save is configured for step 20.
- Hifi production status check: time step 2 reached `NS 2-5`, with `ri_r1=5.609e-04` and no additional linear solver warnings. Disk monitor reports about 17.45 GiB free and `STOP_SIM` is absent.
- Hifi production status check: time step 2 reached `NS 2-6`, with `ri_r1=9.442e-05`; the residual continues to fall and no additional linear solver warnings have appeared. No VTU output yet because production VTK saving starts at step 20.
- Hifi production status check: time step 2 reached `NS 2-9`, with `ri_r1=6.033e-07`; the residual continues to fall and the run remains active. No VTU output yet because production VTK saving starts at step 20.
- Hifi production status check: the run advanced into time step 3 (`NS 3-1`). Time step 2 finished its configured 12 nonlinear iterations with `ri_r1=4.597e-09`; only the earlier `NS 2-1` linear solver warning has occurred. Boundary integral output now contains steps 1 and 2.
- Refreshed live postprocessing under `cases/cow_luisi_mvp/results/post_hifi_np6_live`; `face_flow_timeseries.csv` now contains the first two time steps, and `postprocess_status.json` correctly reports zero VTU files so far.
- Hifi production status check: time step 3 reached `NS 3-5`, with `ri_r1=5.966e-05` after starting from `1.0` at `NS 3-1`. No new linear solver warnings have appeared; the current 6-process formal solve remains the primary run.
- Hifi production status check: time step 3 reached `NS 3-10`, with `ri_r1=1.115e-08`. No new linear solver warnings have appeared. Disk free space is fluctuating but remains above the 5 GiB `STOP_SIM` threshold.
- Hifi production status check: time step 3 finished its configured 12 nonlinear iterations with `ri_r1=3.818e-10`; boundary integral output now contains steps 1-3. Refreshed `cases/cow_luisi_mvp/results/post_hifi_np6_live` so the live flow summaries include step 3.
- Hifi production status check: the run entered time step 4. `NS 4-1` produced a linear solver warning, similar to the earlier `NS 2-1` warning, so the run is being monitored for subsequent residual reduction rather than interrupted.
- Hifi production status check: time step 4 reached `NS 4-2`; `ri_r1` dropped from `1.0` at `NS 4-1` to `3.165e-02` with no new warning at `NS 4-2`. The warning pattern currently matches the earlier step-2 startup warning, so the production solve is continuing.
- Added `scripts/monitor_hifi_solve.py`, a lightweight watcher that can poll the active hifi solve, refresh live postprocessing, and verify the first VTU with PyVista once it appears. A single-poll test succeeded and reported `NS 4-3`; long-running detached execution did not persist in this shell session, so direct monitoring is continuing.
- Hifi production status check: time step 4 reached `NS 4-7`, with `ri_r1=2.031e-05` and no warning after `NS 4-1`. Disk free space remains around 12.8 GiB, `STOP_SIM` is absent, and no VTU has been written yet.
- Hifi production status check: time step 4 finished its configured 12 nonlinear iterations with `ri_r1=5.041e-09`; boundary integral output now contains steps 1-4. Refreshed `cases/cow_luisi_mvp/results/post_hifi_np6_live` so the live flow summaries include step 4.
- Hifi production status check: the run entered time step 5 at `NS 5-1` without a new warning. No VTU output yet because production VTK saving starts at step 20; disk free space remains about 12.8 GiB and `STOP_SIM` is absent.
- Hifi production status check: time step 5 reached `NS 5-4`, with `ri_r1=3.603e-04` and no warnings in step 5 so far. The solve remains active on 6 worker processes; no VTU output yet.
- Hifi production status check: time step 5 reached `NS 5-11`, with `ri_r1=3.191e-09` and no warnings in step 5 so far. Completed steps 1-4 ended with low residuals (`1.303e-11`, `4.597e-09`, `3.818e-10`, and `5.041e-09`). No production VTU has been written yet because the first VTK save is configured for step 20.
- Hifi production status check: time step 5 finished its configured 12 nonlinear iterations with `ri_r1=7.212e-10` and no step-5 warnings; the run advanced to time step 6 (`NS 6-1`). Refreshed `cases/cow_luisi_mvp/results/post_hifi_np6_live`, and boundary flow output now contains steps 1-5. No production VTU has been written yet because the first VTK save is configured for step 20.
- Hifi production status check: time step 6 reached `NS 6-4`, with `ri_r1=2.854e-04` and no step-6 warnings so far. The 6-process production solve remains active; disk free space is about 12.36 GiB and `STOP_SIM` is absent.
- Hifi production status check: time step 6 finished its configured 12 nonlinear iterations with `ri_r1=4.138e-10` and no step-6 warnings; the run advanced to time step 7 (`NS 7-1`). Refreshed `cases/cow_luisi_mvp/results/post_hifi_np6_live`, and boundary flow/pressure output now contains steps 1-6. No production VTU has been written yet because the first VTK save is configured for step 20.
- Hifi production status check: time step 7 reached `NS 7-4`, with `ri_r1=2.953e-04` and no step-7 warnings so far. The 6-process production solve remains active; disk free space is about 12.23 GiB and `STOP_SIM` is absent.
- Hifi production status check: time step 7 finished its configured 12 nonlinear iterations with `ri_r1=1.026e-09` and no step-7 warnings; the run advanced to time step 8 (`NS 8-2` observed after refresh). Refreshed `cases/cow_luisi_mvp/results/post_hifi_np6_live`, and boundary flow/pressure output now contains steps 1-7. No production VTU has been written yet because the first VTK save is configured for step 20.
- Hifi production status check: time step 8 reached `NS 8-6`, with `ri_r1=7.624e-06` and no step-8 warnings so far. The 6-process production solve remains active; disk free space is about 12.30 GiB and `STOP_SIM` is absent.
- Hifi production status check: time step 8 finished its configured 12 nonlinear iterations with `ri_r1=1.058e-09` and no step-8 warnings; the run advanced to time step 9 (`NS 9-1`). Refreshed `cases/cow_luisi_mvp/results/post_hifi_np6_live`, and boundary flow/pressure output now contains steps 1-8. No production VTU has been written yet because the first VTK save is configured for step 20.
- Hifi production status check: time step 9 reached `NS 9-5`, with `ri_r1=4.623e-05` and no step-9 warnings so far. The 6-process production solve remains active; disk free space is about 12.29 GiB and `STOP_SIM` is absent.
- Hifi production milestone: the run advanced past the first VTK save point. `cases/cow_luisi_mvp/solver/6-procs/result_020.vtu` was written and verified with PyVista: 55,604,358 bytes (about 53 MiB), 1,450,418 cells, 271,624 points, point arrays `Velocity`, `Pressure`, `WSS`, `Traction`, `Vorticity`, and `Divergence`, plus cell array `Proc_ID`. The run is active at time step 24 (`NS 24-10` observed), boundary flow/pressure postprocess contains steps 1-23, disk free space is about 17.9 GiB, and `STOP_SIM` is absent.
